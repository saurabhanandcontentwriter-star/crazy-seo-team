name: Actions Health Check

on:
  push:
    branches: [main]
  workflow_dispatch:

permissions:
  contents: read

jobs:
  health:
    runs-on: ubuntu-latest
    timeout-minutes: 5
    steps:
      - name: Runner health
        run: |
          echo "GitHub Actions runner is working"
          node --version
          npm --version
